<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= subject %>
    </title>
    <style>
        body {
            font-family: 'Inter', system-ui, -apple-system, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
            background-color: #0B0F19;
            /* background */
            margin: 0;
            padding: 0;
            color: #FFFFFF;
            /* foreground */
        }

        .container {
            max-width: 600px;
            margin: 0 auto;
            background-color: #0B0F19;
        }

        .card {
            background-color: #0E1422;
            /* card */
            margin: 20px;
            padding: 40px;
            border-radius: 12px;
            border: 1px solid #1C2333;
            /* border */
            box-shadow: 0 8px 24px rgba(0, 0, 0, 0.35);
        }

        .header {
            text-align: center;
            padding-bottom: 20px;
            border-bottom: 1px solid #1C2333;
        }

        .logo {
            font-size: 24px;
            font-weight: bold;
            color: #FFFFFF;
            text-decoration: none;
        }

        .content {
            padding: 30px 0;
            color: #FFFFFF;
        }

        h2 {
            color: #FFFFFF;
            font-size: 20px;
            margin-bottom: 16px;
        }

        p {
            color: #9CA3AF;
            /* gray-400 */
            font-size: 16px;
            line-height: 1.6;
            margin-bottom: 24px;
        }

        .footer {
            text-align: center;
            padding: 20px;
            color: #6B7280;
            /* gray-500 */
            font-size: 12px;
        }

        .footer a {
            color: #2B6EF2;
            text-decoration: none;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="card">
            <div class="header">
                <a href="#" class="logo">
                    <%= companyName %>
                </a>
            </div>
            <div class="content">
                <p><%- message.replace(/\n/g, '<br>' ) %></p>
            </div>
            <div class="footer">
                <p>&copy; <%= new Date().getFullYear() %>
                        <%= companyName %>. All rights reserved.</p>
                <p>
                    <a href="*|UNSUB|*">Unsubscribe</a> *|IF:ARCHIVE_PAGE|* | <a href="*|ARCHIVE_PAGE|*">View this email
                        in your browser</a> *|END:IF|*
                </p>
            </div>
        </div>
    </div>
</body>

</html>